#'Return a fixed version of the following code.The problem is Sequential Search:
def search(x, seq):
    if x < seq[0]:
        return 0
    elif x > seq[-1]:
        return len(seq)
    
    for i in range(len(seq)):
        if x == seq[i]:
            return i
    return -1