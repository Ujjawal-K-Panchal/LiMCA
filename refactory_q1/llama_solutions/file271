#'Return a fixed version of the following code.The problem is Sequential Search:
def search(x, seq):
    count==0
    while count<len(seq):
        if seq[count]<x:
            count+=1
    return count
            
            