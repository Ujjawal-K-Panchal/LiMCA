#'Return a fixed version of the following code.The problem is Top-k:
def top_k(lst, k):
    arranged = []
    while k > 0:
        lst.remove(max(lst))
        arranged.append(max(lst))
        k = k-1
    return arranged
    pass