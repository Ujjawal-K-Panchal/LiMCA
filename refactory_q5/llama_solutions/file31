#'Write a function that takes a list of integers and returns the sum of the first k integers in the list.
def sum_k(lst, k):
    sum = 0
    while lst:
        sum += lst[0]
        lst.remove(lst[0])
    return sum