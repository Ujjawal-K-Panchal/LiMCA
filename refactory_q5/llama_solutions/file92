#'Write a function that takes a list of integers and returns a list of the integers in the list that are not in the list of integers.
def not_in_list(lst):
    new = []
    for i in range(len(lst)):
        if lst[i] not in lst:
            new.append(lst[i])
    return new